---
export interface Props {
    title: string;
    body: string;
    href: string;
    tags: string;
}

const { href, title, body, tags } = Astro.props;
---

<li>
    <a href={`/blog/${href}`}>
        <div>
            <h2>{title}</h2>
            <p>{body}</p>
        </div>
    </a>
</li>